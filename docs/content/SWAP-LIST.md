# Swap List — Placeholders to Replace Before Production Launch

Every known placeholder in the repo, found by sweeping `site/src`, `portal/`, `infra/`, and `docs/` for `PLACEHOLDER`, `555-01*`, `hello@balancebridge.us`, `cal.com/balancebridge`, `illustrative`, `CHANGE_ME`, and the certifications list. Line numbers are as of 2026-07-25.

Priorities: **Blocker** = do not launch production without it · **Should** = fix in the first week · **Nice** = cosmetic / low risk.

| # | Item | Current placeholder value | File(s):line | Replace with | Priority |
|---|---|---|---|---|---|
| 1 | Business phone number | `(512) 555-0146` / `tel:+15125550146` (555 numbers are fictional by definition) | `site/src/config.ts:9-10` | A real business line (Google Voice / OpenPhone / carrier). Update both `phone` and `phoneHref`. Shown sitewide (header, footer, contact, LocalBusiness schema). | **Blocker** |
| 2 | Firm email mailbox | `hello@balancebridge.us` — mailbox does not exist yet | `site/src/config.ts:11`, `portal/src/config.ts:49` (FIRM_INBOX default), `portal/.env.example:69`, `infra/bootstrap.sh:62` (MAIL_FROM) | Create the mailbox (or an alias) at your email host, then keep the address or change it everywhere. Lead alerts and client-message notifications go here — if it doesn't exist, **leads silently vanish**. Also note `infra/bootstrap.sh:63` writes `FIRM_NOTIFY_EMAIL`, but the portal reads `FIRM_INBOX` — add `FIRM_INBOX=` to the server .env files. | **Blocker** |
| 3 | Booking calendar | `https://cal.com/balancebridge/intro` — account not created | `site/src/config.ts:16` | Create the Cal.com account and event type (or swap to Calendly) and confirm the URL. Every "Book a free consultation" CTA points here. | **Blocker** |
| 4 | Certifications list | `QuickBooks Online ProAdvisor`, `Xero Partner`, `AIPB Certified Bookkeeper` — marked "verify before production launch" | `site/src/config.ts:21-26` | Only certifications the firm **actually holds**, with proof (ProAdvisor profile link, AIPB certificate #). Claiming credentials you don't have is a deceptive-practice problem, same family as fake reviews. Delete any that can't be verified. | **Blocker** |
| 5 | Testimonials & case studies | Six fictional testimonials and three fictional case studies ("Marcus T.", "$38,000 in releasable retainage", etc.) | `site/src/data/social-proof.ts` (entire file, flagged at line 1); rendered on `site/src/pages/index.astro:166` and `site/src/pages/about.astro:81` | **FTC note:** these are illustrative composites and the site currently labels them as such ("Illustrative client experiences shown…"). That labeling is why the site is publishable at all today — but fictional reviews presented as real endorsements violate the FTC's Endorsement Guides and the Consumer Review Fairness rules. Before launch either (a) replace with verified quotes from real clients who consented in writing, or (b) hide the testimonial/case-study sections entirely. Do **not** simply remove the "illustrative" disclaimer labels. | **Blocker** |
| 6 | Stripe keys | `STRIPE_SECRET_KEY=CHANGE_ME`, `STRIPE_WEBHOOK_SECRET=CHANGE_ME` in generated server .env | `infra/bootstrap.sh:56-57` (template → `/srv/balancebridge/{uat,prod}/.env`) | Live keys + webhook signing secret per `docs/deploy/DEPLOYMENT.md` §5.2. Until set, billing pages show "not set up" and the Stripe webhook returns 503. | **Blocker** (before billing goes live) |
| 7 | SMTP credentials | `SMTP_HOST/USER/PASS=CHANGE_ME` | `infra/bootstrap.sh:58-61` | A transactional provider (Postmark/SES/Resend/Brevo) + SPF/DKIM DNS. Until set, invites, password resets, and lead alerts are only written to container logs — **clients can never receive an invite**. | **Blocker** |
| 8 | DocuSeal API key | `DOCUSEAL_API_KEY=CHANGE_ME_after_docuseal_first_boot` | `infra/bootstrap.sh:50` | Generate in DocuSeal after its first boot (DEPLOYMENT.md §5.1). Also fix `DOCUSEAL_URL` in the same file to the public `https://sign[-uat].balancebridge.us` origin — the internal `http://docuseal-<env>:3000` breaks the browser signing iframe. | **Blocker** (before e-signatures) |
| 9 | Legal entity name | `Balance Bridge Financial LLC` — "confirm legal entity name" | `site/src/config.ts:5` (used in Terms & Privacy pages) | The exact registered entity name from the Texas SOS filing. | **Blocker** |
| 10 | Terms & Privacy attorney review | Baseline self-drafted policies with `PLACEHOLDER: attorney review` comments; venue/county unconfirmed | `site/src/pages/terms.astro:13,36`, `site/src/pages/privacy.astro:13` | Have a Texas attorney review both pages; confirm dispute venue county at `terms.astro:36`. | Should |
| 11 | Founded year | `2019` | `site/src/config.ts:17` | The real founding year (appears in About/schema). | Should |
| 12 | LinkedIn URL | `https://www.linkedin.com/company/balance-bridge-financial` — page not created | `site/src/config.ts:19` | Create the LinkedIn company page or remove the social link. | Should |
| 13 | UAT basic-auth hash comment | `# PLACEHOLDER hash — generated during bootstrap` | `infra/caddy/Caddyfile:70` | No action — the real hash is injected from `/srv/balancebridge/caddy/.env` at runtime; the comment just explains where it comes from. Consider rewording the comment so future greps don't flag it. | Nice |
| 14 | Demo seed data phone | `+1 (940) 555-0142` (demo client "Lonestar Coffee Co.") | `portal/src/db/seed.ts:65` | Nothing — demo data only exists where `SEED_DEMO=1` (UAT). Just confirm prod keeps `SEED_DEMO=0` (bootstrap already does). | Nice |
| 15 | "Illustrative" examples in blog posts | Worked examples labeled illustrative/fictional in two guides | `site/src/content/blog/cash-flow-forecasting-small-business.md:76`, `site/src/content/blog/catch-up-bookkeeping-guide.md:83` | Nothing required — these are educational examples clearly framed as such, which is fine. Listed only so future sweeps know they were reviewed. | Nice |

## Launch gate

Items **1–9** are the production launch gate. A practical order: create the mailbox (#2) and phone (#1) first since DNS/provider setup has lead time; then Cal.com (#3); then the server-side secrets (#6–8) during the post-deploy configuration pass; verify certifications (#4) and the entity name (#9) whenever the paperwork is at hand; and make the testimonials call (#5 — real quotes vs. hidden section) last, since hiding the sections is a one-line change if real quotes aren't ready.

Related context: `docs/ASSUMPTIONS.md` rows A3 and A5 record why these placeholders were accepted during the build.
